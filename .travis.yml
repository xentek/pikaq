dist: trusty
language: go
go:
    - 1.8
services:
    - rabbitmq
before_install: make setup
install: make install
script: make ci
notifications:
    email:
        on_success: never
        on_failure: always
